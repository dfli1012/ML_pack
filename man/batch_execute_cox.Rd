% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Batch_Run.R
\name{batch_execute_cox}
\alias{batch_execute_cox}
\title{Batch Execution of Cox Model Machine Learning Workflow}
\usage{
batch_execute_cox(
  data,
  N,
  seed = NULL,
  topn = 10,
  stratify = F,
  lambda_type = "lambda.min",
  version = 2,
  n_round = 20,
  bestcut = F,
  train_percent = 0.7
)
}
\arguments{
\item{data}{A dataframe containing survival data (time + status) and features}

\item{N}{Number of repetitions to execute (should match seed vector length if provided)}

\item{seed}{Integer or integer vector specifying random seed(s) (length should match N). Default NULL}

\item{topn}{Number of top features to select. Default 10}

\item{stratify}{Logical indicating whether to stratify sampling by event status. Default FALSE}

\item{lambda_type}{Lambda selection type for glmnet ("lambda.min" or "lambda.1se"). Default "lambda.min"}

\item{version}{Algorithm version to use (1 or 2). Default 2}

\item{n_round}{Number of rounds for internal validation. Default 20}

\item{bestcut}{Logical indicating whether to apply optimal cutpoint for features. Default FALSE}

\item{train_percent}{Proportion of data to use for training (0-1). Default 0.7}
}
\value{
No direct return value. Generates:
- Subdirectories for each seed containing:
  - data_train_result.csv: Training set results
  - data_test_result.csv: Test set results
}
\description{
This function performs repeated execution of Cox model machine learning workflows using different random seeds.
Results are saved in seed-specific folders. Designed for stability assessment through multiple runs.
}
\examples{
\dontrun{
# Run 5 repetitions with automatic seed generation
batch_execute_cox(data = tcga_surv, N = 5, topn = 15)

# Run 3 repetitions with specific seeds and stratification
batch_execute_cox(data = clinical_data, N = 3, seed = c(123, 456, 789),
                 stratify = TRUE, lambda_type = "lambda.1se")
}

}
