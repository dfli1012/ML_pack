% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Batch_Run.R
\name{batch_execute_X}
\alias{batch_execute_X}
\title{Batch Execution of Machine Learning Workflow with Multiple Seeds}
\usage{
batch_execute_X(
  expression_matrix,
  N,
  seed = NULL,
  topn = 10,
  n_round = 20,
  version = 2,
  train_percent = 0.7,
  group_name = 1
)
}
\arguments{
\item{expression_matrix}{Matrix or dataframe containing features (genes/proteins) as columns and samples as rows}

\item{N}{Number of repetitions/runs to execute}

\item{seed}{Initial seed for random number generator (used to generate subsequent seeds). Default 123}

\item{topn}{Number of top features to select. Default 10}

\item{n_round}{Number of rounds for internal validation. Default 20}

\item{version}{Algorithm version to use (1 or 2). Default 2}

\item{train_percent}{Proportion of data to use for training (0-1). Default 0.7}

\item{group_name}{Name/numeric index of the outcome variable column. Default 1}
}
\value{
Invisibly returns AUC matrix. Generates:
- Subdirectories for each seed
- total_auc_result.csv containing:
  - AUC values for each model (rows: glmnet/svm/rf/nnet)
  - Columns for each seed + average column
}
\description{
Repeatedly executes machine learning workflows with different random seeds, stores results in seed-specific folders,
and aggregates performance metrics (AUC) across all runs. Supports glmnet, SVM, Random Forest and Neural Network models.
}
\examples{
\dontrun{
# Basic usage with 5 repetitions
batch_execute_X(expression_matrix = gene_exp, N = 5)

# Custom configuration with 3 repetitions
batch_execute_X(expression_matrix = protein_data, N = 3,version = 2,
               topn = 15, train_percent = 0.8, seed = 456)
}

}
